//this needs finished


function Player() {
    this.name = name;
    this.totalCoins = totalCoins;
    this.star = false;
    this.setName = setName(namePicked);
    this.gotHit = gotHit(status);
    this.gotPoweredUp = 
    }

var penguins = new Party(1000000, "penguins");
var communists = new Party(1000000, "communists");

function createRandomNum(min, max) {
    return (Math.floor((Math.random() * max)) + min);
}

function setName(namePicked);{
this.name = name;
}
var 
function gotHit()


